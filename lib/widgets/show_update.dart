import 'package:flutter/material.dart';

class ShowUpdate extends StatelessWidget {
  const ShowUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return CircleAvatar( );
  }
}
 